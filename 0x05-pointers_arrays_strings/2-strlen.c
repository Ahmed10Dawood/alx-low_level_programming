#include "main.h"

/**
 * _strlen - count the length of string
 *
 * @s: input string
 *
 * Return: length of string
*/

int _strlen(char *s)
{
	int counter;

	for (counter = 0; *s != '\0'; s++)
		conter++;
	return (counter);
}
